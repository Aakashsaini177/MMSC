{
  "name": "mmsc-root",
  "version": "1.0.0",
  "description": "Root package.json for MMSC deployment",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix client && npm run build --prefix client",
    "start": "npm start --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
